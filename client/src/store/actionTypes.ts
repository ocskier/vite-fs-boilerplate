/**
 * All global store action type string constants
 */
const ACTION_TYPES = {
  // Action types on store related to the logged in user
  USER: {
    SET_SESSION_ID: 'SET-SESSION-ID',
    SET_USER: 'SET-USER',
  },
};

export default ACTION_TYPES;
